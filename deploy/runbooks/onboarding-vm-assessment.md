# Retired: onboarding VM assessment

The assessed `core-central-vm` host is a sandbox. It is not evaluated or remediated as a production
target, and none of its source, configuration, credentials or data promotes to production.

See `onboarding-rollout.md` and `apps/backend/docs/gcp-deployment.md` for the Cloud Run/Cloud SQL
production path. This tombstone remains so older links cannot lead to unsafe VM instructions.
