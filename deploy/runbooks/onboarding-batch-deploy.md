# Retired: VM onboarding batch deployment

This VM procedure is retired. `core-central-vm` is a sandbox, not production.

Use `onboarding-rollout.md` for Cloud Run production deployment. Do not deploy production images,
configuration or migrations to the VM, and do not restore its Postgres data into Cloud SQL.
