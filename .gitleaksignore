# Reviewed and allowlisted — each entry is deliberate, not a leak.
#
# Firebase WEB API keys are public by design: they ship inside every frontend
# bundle and are restricted by Firebase Auth authorized domains, not secrecy.
# ⚠️ The trailing number is the LINE of the match — editing above one of these
# keys shifts it and the entry silently stops matching (that failed a PR's
# secret scan on 25 Aug 2026). Re-run `gitleaks dir .` after touching them.
apps/frontend/.env.example:gcp-api-key:26
scripts/configure-github-env-vars.sh:gcp-api-key:31
deploy/runbooks/staging-call-battery.md:gcp-api-key:11
#
# Fake credentials used to test that the log redaction actually redacts.
apps/backend/src/utils/logger.test.ts:jwt:38
apps/backend/src/utils/logger.test.ts:stripe-access-token:66
#
# Documentation placeholder (<retell-api-key>), not a value.
apps/backend/docs/retellai-phase1.md:generic-api-key:201
